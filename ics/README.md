## INSTALLATIONS
<u> Python: </u>

Install for computer

<u> Flask: </u>
```bash
pip3 install flask
```

## HOW TO RUN (FROM TERMINAL)

<u> For ocr-v1 (example) </u>
```bash
python3 ~/your-folder/rcpt_bench_check.py P0HPJH_output.json
```

<u> For ocr-v2, ocr-v3, ocr-v4 (example) </u>
```bash
python3 ~/your-folder/final_ocr.py ~/your-folder/invoice-ex
```

### *More information on executing is provided in each foldr (each Python file)*